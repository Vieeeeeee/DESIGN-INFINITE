# ============================================
# JWT 配置
# ============================================
# 请修改为随机字符串，可使用: openssl rand -base64 32
JWT_SECRET=your-jwt-secret-here

# ============================================
# 邮件配置 (SMTP)
# ============================================
# 支持 QQ邮箱、163邮箱、Gmail 等
# QQ邮箱需要使用授权码而非密码
SMTP_HOST=smtp.example.com
SMTP_PORT=465
SMTP_USER=your-email@example.com
SMTP_PASS=your-smtp-password

# ============================================
# CORS 配置
# ============================================
# 前端地址，用于跨域配置
FRONTEND_URL=http://localhost:5173

# ============================================
# 积分系统配置
# ============================================
INVITE_REWARD_POINTS=500
INVITE_CODES_PER_USER=10

# ============================================
# Google Vertex AI 配置
# ============================================
# Service Account JSON 文件路径
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
# GCP 项目 ID
VERTEX_AI_PROJECT=your-gcp-project-id
# 区域
VERTEX_AI_REGION=global
# 模型名称
VERTEX_AI_MODEL=gemini-3-pro-image-preview

# ============================================
# 静态文件存储配置 (本地开发用)
# ============================================
# 本地生成图片存储目录 (默认: /www/generator/runtime/generated)
# STATIC_DIR=./generated
# 本地生成图片 URL 前缀 (默认: https://api.abdc.online/generated)
# STATIC_URL_BASE=http://localhost:3001/generated

